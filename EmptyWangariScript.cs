using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// This is an empty script, so that another script can find Wangari's game object by script name

public class EmptyWangariScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
